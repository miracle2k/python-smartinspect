Files in this directory must take a SmartInspect connection
string as a first argument, and  subsequently use it during 
their test.

If the memory protocol is used, the testrunner expects the 
current log content on stdout, otherwise stdout is ignored.